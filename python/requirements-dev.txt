# Development/test dependencies, not used for production packaging.
-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0

# Lint / format (see python/ruff.toml)
ruff>=0.6.0

# Desktop packaging (moved from requirements.txt)
pyinstaller==6.20.0
