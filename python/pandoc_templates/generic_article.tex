\documentclass[$if(fontsize)$$fontsize$,$endif$]{article}
$if(lang)$
\usepackage[$lang$]{babel}
$endif$
\usepackage[utf8]{inputenc}
\usepackage{etoolbox}
% Pandoc inserts \tightlist between list items — define it if missing
\providecommand*\tightlist{\setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}
\usepackage{amsmath,amssymb}
\usepackage{framed}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\rhead{\thepage}
\lhead{\thepage}

$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(date)$
\date{$date$}
$endif$
$if(keywords)$
\usepackage[$for(keywords)$$keywords$$sep$,$endfor$]{glossaries}
$endif$

\begin{document}
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$
$if(maketitle)$
\maketitle
$endif$
$if(toc)$
\tableofcontents
$endif$

$body$

$if(bibliography)$
\bibliographystyle{plain}
\bibliography{$bibliography$}
$endif$

\end{document}
